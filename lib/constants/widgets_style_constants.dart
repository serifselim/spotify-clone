import 'package:flutter/material.dart';
import 'package:spotify_clone/constants/color_constants.dart';

const musicItemDecoration = BoxDecoration(
  color: colorMineShaft,
  borderRadius: BorderRadius.all(
    Radius.circular(4.0),
  ),
);
